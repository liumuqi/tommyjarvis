提效主要是多方向仲裁流程 
收益 多方向仲裁提升70%,标注多方向提升 20%

```
标注页面：
P0 1、添加link时，只需要点击一次确定，link信息及状态信息都可以同步添加（现在需要点击两次，第一次无论是否选状态信息，添加 后都是无法判断）
P0 2、标注框页面，支持单击link时，在地图页面展示出来，方便标注人员查看已标注方向，并可以在标注框页面进行状态选择及删除 操作
(质检和仲裁 (两个人的结果link操作显示问题) )
P0 3、仲裁标注页面，可将标注人员已标注link一一对应起来，并且只仲裁标注不一致的方向，支持单击link时，在地图页面展示（并且标注人员已标注一致的case在 仲裁人员界面为有值的状态，并可更改，//需要看一下合理性，只对多方向有效）
 ( 两个结果 差异化高亮显示 )
P0 4、支持右键拾取坐标点的信息

P0 5、街景查看时，高德地图出现对应位置的标记

P1 6、接入导航，可以设置起终点规划路线
P1 7、标注页面默认上次选择的场景(往后放)

P0 1、拉框测距会出现删除的情况，且有时未点击拉框按钮，也会触发拉框操作 (前端-无法删除-不好复现)
P0 2、GPS信息展示框、linkID展示框偶尔会出现叉不掉的情况(前端-无法删除)

P0 3、质检页面同一case对比质检前与标注结果时，会考虑 (后台 确认 多方向一致问题）
P1 4、拉框页面必须得点两次才可以关闭 (必现)
5. 质检/仲裁 页面链接分享问题

任务监控页面：
P0 1、点击过滤、仲裁、合格、二次标注后，页面不筛新 (acl控制 过期问题)
P1 2、可支持无效任务包的删除（可以设置高级权限，指定人员才可以删除，感觉现在无效任务包太多了）(删完的任务包内的case需要重新指派)
P0 3、在入库前质检时，支持选择目标任务包进行质检入库（目前是点击合格后的任务包会直接质检入库，但在多个项目并行时，很容易因误操作导致非目标项目的任务包被入库）

筛选指派页面：
P1 1、支持城市选择时的模糊匹配 (前端)

真值查看页面：
P1 1、支持城市选择时的模糊匹配(前端)
P1 2、支持选择框的一键删除，主要包括真值状态、多方向状态、标注状态、Rank等
```

socol标注流程定制化 <待与技术和标注同学确定使用场景>  
 
#后续方向
标注流程规范化->提效, 从指派到标注可满足自定义化任务

童虎数据存储扩容-分库
